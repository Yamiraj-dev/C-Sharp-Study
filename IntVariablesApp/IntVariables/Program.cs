//integers are whole numbers, no decimal points


int age = 0;

int ageInTenYears = age + 10;

age = 25;

//signed Int32 - signed meaning positive or negative integers but only around 2 billion
//unsigned - around 4 billion integers

//signed Int32 - 32 bits meaning 2^32 which gives us around 4 billion memory stores, which we divide equally for negative and positive integers

Console.WriteLine(age);
Console.WriteLine(ageInTenYears);