// Boolean is either true or false

bool isComplete = false;

isComplete = !isComplete; // "!" is an operator that flips the value of our variable in this case from false to true
                          
Console.WriteLine(isComplete);
Console.WriteLine(!isComplete);// this operator can also be used in CW but this does not modify the original variable 